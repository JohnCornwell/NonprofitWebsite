/* Interface describing the donates table in the database. This
 * interface is used to assist the donor pages in displaying
 * donations made by the donor.
 */

export interface Donates {
  UserID: number,
  DonationID: number
}
